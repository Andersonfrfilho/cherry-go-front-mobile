export enum TRANSPORT_TYPE_PROVIDER_TRANSLATE_ENUM {
  client = 'Cliente',
  provider = 'Provedor',
  uber = 'Uber',
}

export enum TransportTypeProviderEnum {
  client = 'client',
  provider = 'provider',
  uber = 'uber',
}
