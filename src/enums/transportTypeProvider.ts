export enum TransportTypeProviderTranslateEnum {
  client = 'Cliente',
  provider = 'Próprio',
  uber = 'Uber',
}

export enum TransportTypeProviderEnum {
  client = 'client',
  provider = 'provider',
  uber = 'uber',
}
